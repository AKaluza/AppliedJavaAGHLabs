### Read Object From JSON Array
The Jackson ObjectMapper can also read an array of objects 
from a JSON array string.
Reading arrays of objects also works with other JSON sources than a string. 
For instance, a file, URL, InputStream, Reader etc.

Your task is to fix `ReadObjectArray` class.