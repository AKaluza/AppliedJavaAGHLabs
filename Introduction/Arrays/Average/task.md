You are given an array of five floating-point numbers:

double[] nums = {10.1, 11.2, 12.3, 13.4, 14.5};


Your job is to:

Loop through all elements of the array using a for loop.

- Compute the total sum of all numbers. 
- Compute the average by dividing the sum by the number of elements. 
- Return the result in the format:
- In Main print: Average is X

Where X is the computed average.