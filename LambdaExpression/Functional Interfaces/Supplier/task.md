# Supplier<T>

## Goal
Create:
```java
Supplier<Integer> random0to9
```
It returns a random integer from 0 to 9 (inclusive) each time get() is called.

<div class="hint">
  You can use new java.util.Random().nextInt(10) or ThreadLocalRandom.current().nextInt(10)
</div>
