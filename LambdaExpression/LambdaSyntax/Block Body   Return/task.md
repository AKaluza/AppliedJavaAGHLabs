# Lambda Syntax — Block Body & `return`

## Goal
Implement:
```java
Function<String,String> greeter = name -> {
  // trim input and return "Hello, <name>!"
};
```
## Hints
Block body needs { } and return.
