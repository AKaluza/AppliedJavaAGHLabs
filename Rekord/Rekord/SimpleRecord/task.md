# Task 1: Simple Record – `Book`

### Goal
Learn how to define a simple Java record and use its automatically generated methods.

### Instructions
1. Define a record `Book` with two components:
  - `String title`
  - `String author`

2. Run the provided `Task` class and observe:
  - how `toString()` prints data,
  - how `equals()` compares two record instances.